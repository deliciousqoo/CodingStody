/*
* 백준2563 색종이
* 2차원 배열
* 20240220
*/
#include <iostream>
#include <vector>

using namespace std;

int main() {

	/*
	vector<vector<int>> arr(100);
	for (int i = 0; i < 100; i++) {
		arr[i].resize(100, 0);
	}*/

	int arr[101][101] = { 0, };

	int N;
	cin >> N;

	int a, b;
	for (int i = 0; i < N; i++) {
		cin >> a >> b;
		for(int j = b; j<a+10;j++)
	}

	

	return 0;
}